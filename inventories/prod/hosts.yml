# Update ansible_host to the target production VPS before running these playbooks.
all:
  hosts:
    velveta_prod:
      ansible_host: 45.195.75.81
      ansible_user: root
      ansible_python_interpreter: /usr/bin/python3
      # ansible_ssh_private_key_file: /root/.ssh/id_ed25519
